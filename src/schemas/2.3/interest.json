{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "http://ciam.nestle.com/schemas/interest.json#",
    "title": "interest",
	"description": "data/interest subschema",
	"type": "object",
	"properties": {
        "interestCode": {"type": "string", "maxLength":200,"description": "pce: INTEREST_CODE"},
        "answerDetails": {"type": "string", "maxLength":200,"description": "pce: ANSWER_DETAILS"},
        "creationDate": {"type": "string", "format": "date-time"},
        "lastUpdateDate": {"type": "string", "format": "date-time"}
    },
    "additionalProperties": false,
    "required": ["interestCode", "answerDetails"]
}
